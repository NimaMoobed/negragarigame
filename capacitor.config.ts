import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.negargari.iranian',
  appName: 'رنگ‌آمیزی هنر نگارگری ایرانی',
  webDir: 'dist'
};

export default config;
